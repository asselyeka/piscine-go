package piscine

//import "fmt"

func StrLen(str string) int {
	var count int
	for index := range str {
		//fmt.Printf("letter %v, index %v", rune(word), index)
		count = index
		//fmt.Println()

	}

	return count
}
